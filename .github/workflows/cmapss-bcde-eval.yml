name: cmapss-bcde-eval

on:
  push:
    branches:
      - cmapss-bcde-eval
  workflow_dispatch:

concurrency:
  group: cmapss-bcde-eval-${{ github.ref }}
  cancel-in-progress: true

jobs:
  supersede-old-full-runs:
    runs-on: ubuntu-latest
    steps:
      - name: Supersede obsolete duplicate full runs
        run: echo "Obsolete duplicate full runs are superseded; formal 3-seed script remains in cmapss_experiment.py."
